[pytest]
testpaths = book/tests/planes
python_files = test_*.py
addopts = -ra --strict-markers --tb=short -p book.tests.support
markers =
    system: system-dependent tests (e.g., macOS/libsandbox)
    smoke: fast smoke checks (subset of contracts)

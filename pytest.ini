[pytest]
testpaths = book/integration/tests
python_files = test_*.py
addopts = -ra --strict-markers --tb=short -p book.integration.support
markers =
    system: system-dependent tests (e.g., macOS/libsandbox)
    smoke: fast smoke checks (subset of contracts)

;; libsandbox-encoder Phase A: network arg matrix (socket-protocol numeric; attempts non-zero high byte)
;; World: sonoma-14.4.1-23E224-arm64-dyld-2c0602c5

(version 1)
(deny default)

(allow network-outbound
  (socket-protocol 256))

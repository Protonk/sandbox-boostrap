;; libsandbox-encoder Phase A: network arg matrix (socket domain+type+proto, require-any)
;; World: sonoma-14.4.1-23E224-arm64-dyld-a3a840f9

(version 1)
(deny default)

(allow network-outbound
  (require-any
    (socket-domain AF_INET)
    (socket-type SOCK_STREAM)
    (socket-protocol IPPROTO_TCP)))


;; libsandbox-encoder Phase A: network arg matrix (socket domain+proto, require-any)
;; World: sonoma-14.4.1-23E224-arm64-dyld-2c0602c5

(version 1)
(deny default)

(allow network-outbound
  (require-any
    (socket-domain AF_INET)
    (socket-protocol IPPROTO_TCP)))

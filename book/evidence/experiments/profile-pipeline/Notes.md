# Notes

- Consolidated encoder write tracing, layout decoding, op-table buckets, vocab alignment, and `sandbox_init*` handle packing under `book/evidence/experiments/profile-pipeline/` (legacy paths are symlinked for compatibility).

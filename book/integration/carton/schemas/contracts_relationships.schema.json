{
  "type": "object",
  "required": ["schema_version", "world_id", "inputs", "relationships"],
  "properties": {
    "schema_version": {"type": "integer"},
    "world_id": {"type": "string"},
    "inputs": {"type": "array", "items": {"type": "string"}},
    "relationships": {"type": "object"}
  }
}

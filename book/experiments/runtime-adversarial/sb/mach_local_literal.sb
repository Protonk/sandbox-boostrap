(version 1)
(allow default)

;; Allow a specific local-name mach service; deny a bogus one.
(allow mach-lookup (local-name "com.apple.cfprefsd.agent"))
(deny mach-lookup (local-name "com.apple.sandboxadversarial.fake"))

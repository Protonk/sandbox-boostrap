(version 1)
(deny default)

;; Bootstrap to allow process startup
(import "bsd.sb")

(allow process-exec
  (literal "/usr/bin/python3"))

(allow file-read*
  (subpath "/tmp/sbpl_rt/param_root"))
(allow file-write*
  (subpath "/tmp/sbpl_rt/param_root"))

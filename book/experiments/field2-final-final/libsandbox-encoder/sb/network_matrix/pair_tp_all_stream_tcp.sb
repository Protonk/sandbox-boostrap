;; libsandbox-encoder Phase A: network arg matrix (socket type+proto, require-all)
;; World: sonoma-14.4.1-23E224-arm64-dyld-2c0602c5

(version 1)
(deny default)

(allow network-outbound
  (require-all
    (socket-type SOCK_STREAM)
    (socket-protocol IPPROTO_TCP)))

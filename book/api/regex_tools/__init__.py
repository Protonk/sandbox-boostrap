"""
AppleMatch regex helpers for legacy (decision-tree) sandbox profiles.

Functions are exposed via the CLI scripts:
- `extract_legacy.py` – pull .re blobs out of early profiles.
- `re_to_dot.py` – render compiled .re blobs to Graphviz .dot.
"""

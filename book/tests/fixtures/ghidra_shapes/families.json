{
  "schema_version": "1.0",
  "mode": "task",
  "overrides": {},
  "notes": [
    "Maintenance-only family map for reporting/pruning; tests ignore this file.",
    "Default mode groups entries by manifest 'task'. Use overrides to pin specific entries to a family."
  ]
}

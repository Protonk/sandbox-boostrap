{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "book/tests/fixtures/ghidra_shapes/manifest.schema.json",
  "type": "object",
  "required": ["schema_version", "world_id", "build_id", "entries"],
  "properties": {
    "schema_version": {"type": "string"},
    "world_id": {"type": "string"},
    "build_id": {"type": "string"},
    "entries": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "task", "output_path", "snapshot_path"],
        "properties": {
          "name": {"type": "string"},
          "task": {"type": "string"},
          "output_path": {"type": "string"},
          "snapshot_path": {"type": "string"},
          "required": {"type": "boolean"},
          "shape": {
            "type": "object",
            "properties": {
              "list_length": {"type": "boolean"},
              "max_list_items": {"type": ["integer", "null"]}
            },
            "additionalProperties": false
          },
          "notes": {"type": "string"}
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}

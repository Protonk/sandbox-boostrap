# Anchor mappings

Stable anchor-derived mappings live here.

Current artifacts:
- `anchor_field2_map.json` – anchor → `field2` hints derived from `probe-op-structure` anchor hits.
- `anchor_filter_map.json` – anchor → filter-ID/name map (confidence varies by anchor; see status fields).

Planned artifacts:

# System profiles

Canonical system-profile digests live here.

Current artifact:
- `digests.json` – per-profile digest (op-table buckets, tag counts, literal sample, sections) for curated system blobs (airlock, bsd, sample) on this host/build.

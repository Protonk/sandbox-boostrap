# Runtime probes

Runtime probe outputs will live here once the `runtime-checks` experiment runs.

Expected artifacts:
- Allow/deny traces or logs keyed to the profiles under test.
- Summaries tying runtime observations back to decoded graph expectations.

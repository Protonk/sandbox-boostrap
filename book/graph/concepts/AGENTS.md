# Agents in `book/graph/concepts/`

Purpose:
- Validation tooling lives under `validation/` (code here; fixtures/outputs under `book/evidence/graph/concepts/validation/`).
- Concept inventory sources + generated JSON live under `book/evidence/graph/concepts/` (`CONCEPT_INVENTORY.md`, `concepts.json`, `concept_map.json`, `concept_text_map.json`).
- Strategies (`book/evidence/graph/concepts/validation/strategies.json`) are generated by the Swift tool.

Do:
- Edit the concept inventory markdown when changing definitions or validation intent.
- Regenerate JSON via `swift run` from `book/graph/`.

Don’t:
- Manually edit generated JSON; instead adjust sources and rerun.
- Invent new concept names without aligning with `CONCEPT_INVENTORY.md`.

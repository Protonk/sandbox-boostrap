;; libsandbox-encoder Phase A: network arg matrix (socket domain+proto, require-all, reordered)
;; World: sonoma-14.4.1-23E224-arm64-dyld-a3a840f9

(version 1)
(deny default)

(allow network-outbound
  (require-all
    (socket-protocol IPPROTO_TCP)
    (socket-domain AF_INET)))
